query SafetyCheckInStatus($shipmentId: ID!) {
  safetyCheckInStatus(shipmentId: $shipmentId) {
    customer {
      id
      role
      confirmedAt
    }
    carrier {
      id
      role
      confirmedAt
    }
  }
}
