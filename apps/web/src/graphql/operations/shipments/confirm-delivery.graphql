mutation ConfirmDelivery($shipmentId: ID!, $input: ConfirmDeliveryInput!) {
  confirmDelivery(shipmentId: $shipmentId, input: $input) {
    id
    confirmed
    issueDescription
    confirmedAt
  }
}
