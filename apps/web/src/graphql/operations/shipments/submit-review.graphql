mutation SubmitReview($shipmentId: ID!, $rating: Int!, $tagIds: [ID!]) {
  submitReview(shipmentId: $shipmentId, rating: $rating, tagIds: $tagIds) {
    id
    reviewerRole
    rating
    tags
    createdAt
  }
}
