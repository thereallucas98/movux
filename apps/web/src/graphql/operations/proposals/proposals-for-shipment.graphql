query ProposalsForShipment($shipmentId: ID!) {
  proposalsForShipment(shipmentId: $shipmentId) {
    id
    status
    carrierId
    carrierName
    carrierPhone
    carrierAvgRating
    priceInCents
    currentAttempt
    currentAttemptResponseType
    expiresAt
    createdAt
  }
}
