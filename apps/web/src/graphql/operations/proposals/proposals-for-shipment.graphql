query ProposalsForShipment($shipmentId: ID!) {
  proposalsForShipment(shipmentId: $shipmentId) {
    id
    status
    carrierId
    carrierName
    priceInCents
    currentAttempt
    expiresAt
    createdAt
  }
}
