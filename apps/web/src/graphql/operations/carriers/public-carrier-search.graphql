query PublicCarrierSearch($cityId: ID!, $vehicleCategoryId: ID) {
  publicCarrierSearch(cityId: $cityId, vehicleCategoryId: $vehicleCategoryId) {
    userId
    firstName
    vehicleCategoryName
    avgRating
    totalShipments
  }
}
