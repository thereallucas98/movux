# S0-T3 — Todo

- [ ] `middleware.ts` — verifica cookie `movux_token`, redireciona por role, protege rotas
- [ ] `lib/session.ts` — `getSession`, `setSession`, `clearSession`
- [ ] Layout público `(auth)` criado
- [ ] Layout customer criado com guard de role
- [ ] Layout carrier criado com guard de role
- [ ] Layout admin criado com guard de role
- [ ] Login page funcional (consume `/api/auth/login`, seta cookie, redireciona)
- [ ] Register page funcional (consume `/api/auth/register`, seta cookie, redireciona)
- [ ] Logout implementado (limpa cookie, redireciona para `/login`)
- [ ] 8 placeholder pages criadas (sem erro 404)
- [ ] Navbar com nome do usuário e botão logout em cada layout protegido
- [ ] QA roteiro completo passando
